#include<stdio.h>
#include<stdlib.h>
int main(){
    int x, i, a, opcao;
    char **p;
    scanf("%d", &x);
    for(i = 0; i < strlen(*p); i++){

    }
    for(i = 0; i < x; i++){
        scanf("%c", *p[i]);
        *p = (char*)malloc(sizeof(char)*(strlen(*p[i])));
    }
    for(i = 0; i < x; i++){
    }
}